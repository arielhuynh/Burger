
<div class="jumbotron">
  <div class="container">
    <h1 class="display-4">Eat Da Burg</h1>
    <p class="lead">Make a burger, eat a burger. Womp Womp!</p>
  </div>
</div>


    <div class="col-md-6 burgermenu">
      <h2>Burger Menu</h2>
      <ul>
        {{#each burgers}} {{#unless devoured}} {{> burgers/burger-block devoured=true}} {{/unless}} {{/each}}
      </ul>
    </div>



    <div class="col-md-6">
      <h2>ChOmP cHoMp!</h2>
      <ul>
        {{#each burgers}} {{#if devoured}} {{> burgers/burger-block devoured=false}} {{/if}} {{/each}}
      </ul>
    </div>

    <div class="col-md-12 burgercreative">
		<img src="/assets/img/burger.png">
      <h2>Let's get Creative. Make your own burger!</h2>
      <form class="create-form">

        <div class="form-group">
          <label for="br">Burger Name:</label>
          <input type="text" id="br" name="burger_name">
        </div>

        <div class="form-group">
          <button type="submit" class="center-block btn btn-default"> Add Burger<name="devoured" value="0" >

          </button>

        </div>


      </form>
    </div>
  </div>
</div>
</div>